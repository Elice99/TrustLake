fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic-settings==2.15.0
# pydantic/pydantic-core pinned explicitly (not just pulled in via
# fastapi's own pydantic>=2.9.0) to avoid pip's resolver backtracking
# through the full pydantic version range searching for a match — on a
# slow/unstable connection this can time out mid-search and surface as
# a misleading "ResolutionImpossible" conflict that isn't a real
# version incompatibility. Values below match what fastapi==0.141.1
# resolves to anyway; if you bump fastapi or pydantic-settings, check
# whether these need bumping too.
pydantic==2.13.5
pydantic-core==2.46.5
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
alembic==1.19.2
email-validator==2.3.0
